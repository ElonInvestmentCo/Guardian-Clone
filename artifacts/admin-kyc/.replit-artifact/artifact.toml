kind = "api"
previewPath = "/admin-kyc-internal"
title = "Guardian Admin — KYC (Dev Server)"
version = "1.0.0"
id = "artifacts/admin-kyc"

[[services]]
localPort = 8080
name = "web"
paths = ["/admin-kyc-internal"]

[services.env]
PORT = "8080"
BASE_PATH = "/admin-kyc/"
API_PORT = "3001"

[services.development]
run = "echo '[DISABLED] Use the named Admin KYC workflow instead.' && sleep infinity"
