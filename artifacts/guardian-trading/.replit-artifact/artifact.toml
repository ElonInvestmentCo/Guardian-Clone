kind = "api"
previewPath = "/guardian-trading-internal"
title = "Guardian Trading (Dev Server)"
version = "1.0.0"
id = "artifacts/guardian-trading"

[[services]]
localPort = 3000
name = "web"
paths = ["/guardian-trading-internal"]

[services.env]
PORT = "3000"
BASE_PATH = "/"
API_PORT = "3001"

[services.development]
run = "echo '[DISABLED] Use the named Guardian Trading workflow instead.' && sleep infinity"
