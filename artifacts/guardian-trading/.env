# Leave VITE_API_URL empty for Replit / same-origin deployments (Express serves both frontend and API).
# Set it in your hosting dashboard (e.g. Vercel env vars) only if the API is on a separate domain.
VITE_API_URL=
